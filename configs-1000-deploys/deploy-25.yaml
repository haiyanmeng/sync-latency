apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx25
  namespace: bookstore
spec:
  replicas: 1
  minReadySeconds: 9
  selector:
    matchLabels:
      app: nginx25
  template:
    metadata:
      labels:
        app: nginx25
    spec:
      containers:
      - image: nginx:1.7.9
        name: nginx25
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
          protocol: TCP
