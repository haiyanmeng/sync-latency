apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx664
  namespace: bookstore
spec:
  replicas: 1
  minReadySeconds: 9
  selector:
    matchLabels:
      app: nginx664
  template:
    metadata:
      labels:
        app: nginx664
    spec:
      containers:
      - image: nginx:1.7.9
        name: nginx664
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
          protocol: TCP
