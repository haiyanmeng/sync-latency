apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx169
  namespace: bookstore
spec:
  replicas: 1
  minReadySeconds: 9
  selector:
    matchLabels:
      app: nginx169
  template:
    metadata:
      labels:
        app: nginx169
    spec:
      containers:
      - image: nginx:1.7.9
        name: nginx169
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
          protocol: TCP
