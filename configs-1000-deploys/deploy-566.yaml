apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx566
  namespace: bookstore
spec:
  replicas: 1
  minReadySeconds: 9
  selector:
    matchLabels:
      app: nginx566
  template:
    metadata:
      labels:
        app: nginx566
    spec:
      containers:
      - image: nginx:1.7.9
        name: nginx566
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
          protocol: TCP
