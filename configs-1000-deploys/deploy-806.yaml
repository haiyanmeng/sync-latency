apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx806
  namespace: bookstore
spec:
  replicas: 1
  minReadySeconds: 9
  selector:
    matchLabels:
      app: nginx806
  template:
    metadata:
      labels:
        app: nginx806
    spec:
      containers:
      - image: nginx:1.7.9
        name: nginx806
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
          protocol: TCP
